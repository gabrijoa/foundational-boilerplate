/**
 * @fileoverview Main application component
 * @description This file defines the root component of the React application.
 * This is a clean template version with minimal content.
 * @author Joa Gabri
 * @version 1.0.0
 */

/**
 * Main App component
 * @description Root component that renders a simple application template.
 * This is the starting point for your React application.
 * @component
 * @returns {JSX.Element} Simple application template
 * @example
 * // This component is automatically rendered by main.tsx
 * // Shows a simple "app" heading as a starting point
 */
function App() {
  return (
    <div className="min-h-screen">
      <h1>app</h1>
    </div>
  );
}

export default App;